# Security: Disable install scripts to prevent malicious code execution
# Scripts can still be run manually with --ignore-scripts=false if needed
ignore-scripts=true

# Security: Use exact lockfile during CI/CD to prevent unexpected updates
package-lock=true

# Performance: Use legacy peer deps to avoid conflicts
legacy-peer-deps=false

# Registry
registry=https://registry.npmjs.org/
